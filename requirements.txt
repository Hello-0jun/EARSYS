# EARSYS dependencies
# Shared for amd64 and arm64.
# Supports the Python range declared in pyproject.toml.

# Computer vision / numerical computing
mediapipe==0.10.18
opencv-python>=4.8.0
numpy>=1.24.0

pytest>=8.0.0
