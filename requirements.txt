# EARSYS dependencies
# Shared for amd64 and arm64 (mediapipe==0.10.18 official manylinux wheel supported)
# Requires Python 3.12

# Computer vision / numerical computing
mediapipe==0.10.18
opencv-python>=4.8.0
numpy>=1.24.0

pytest>=8.0.0
